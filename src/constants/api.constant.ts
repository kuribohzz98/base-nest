export const MaxLimitPagination = 50;

export const MAX_SIZE_FILE = 10 * 1024 * 1024; // 10Mb
export const MAX_SIZE_GIF = 500 * 1024; // 500KB

export const MIN_SIZE_IMAGE = {
	GENERAL: 1024 * 1024, // 1MB,
};

export const MIN_SIZE_IMAGE_MINIFY = {
	GENERAL: 1024 * 1024, // 1MB,
};

export enum EFolderType {
	AVATARS = 'Avatars',
}
